// POST /command      — Parent sends a remote command (block/unblock app)
// GET  /commands/:deviceId — Child polls for pending commands
// POST /commands/:id/ack  — Child acknowledges command was executed
